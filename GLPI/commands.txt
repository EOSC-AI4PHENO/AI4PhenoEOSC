CREATE DATABASE glpi_db;

CREATE USER 'nazwa_uzytkownika'@'localhost' IDENTIFIED BY 'twoje_haslo';


GRANT ALL PRIVILEGES ON glpi_db.* TO 'glpi_user'@'localhost' IDENTIFIED BY 'twoje_haslo';
FLUSH PRIVILEGES;


-- Utworzenie bazy danych o nazwie `glpi_database`
CREATE DATABASE glpi_database CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Utworzenie użytkownika o nazwie `glpi_user` z hasłem `your_password`
CREATE USER 'glpi_user'@'localhost' IDENTIFIED BY 'Qwerty_123';

-- Nadanie wszystkich uprawnień użytkownikowi `glpi_user` do bazy `glpi_database`
GRANT ALL PRIVILEGES ON glpi_db.* TO 'glpi_user'@'localhost';

-- Przeladowanie uprawnien
FLUSH PRIVILEGES;


sudo apt-get update
sudo apt-get install php-intl
sudo service apache2 restart

sudo chown -R www-data:www-data /var/www/glpi

sudo chmod -R 755 /var/www/glpi

Default logins / passwords are:


glpi/glpi for the administrator account
QLTBbyHvDYUJ
tech/tech for the technician account
4vMNWxaMknXx
normal/normal for the normal account
EtBNQxL7UkWn
post-only/postonly for the postonly accoun
xhgeDXhqVzSy

password:QLTBbyHvDYUJ

http://10.0.20.40:8881/index.php


